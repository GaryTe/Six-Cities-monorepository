export type AccessToken = {
  accessToken: string
}

export type TokenPayload = {
  id: string;
  email: string;
  name: string;
  avatar: string;
  typeUser: string;
}

export type Author = {
  _id: string;
  email: string;
  name: string;
  avatar: string;
  typeUser: string;
}
